/**
 * @file simple_example.js
 * @brief JavaScript转C++示例
 */

// 简单的算术运算
function calculateDamage(base, multiplier) {
    return base * multiplier;
}

let damage = calculateDamage(100, 1.5);

// 变量声明
let health = 100;
let mana = 50;
let name = "Player";

// 数组
let inventory = ["sword", "shield", "potion"];

// 对象访问（简化版）
// let player = {
//     name: "Hero",
//     level: 10
// };
