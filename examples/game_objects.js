// Game object example with multiple objects

let player = {
    name: "Ash",
    level: 25,
    health: 100
};

let enemy = {
    name: "Gary",
    level: 30,
    health: 150
};

// Test member access
let playerName = player.name;
let enemyHealth = enemy.health;
